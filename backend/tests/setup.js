module.exports = async () => {
  // This function runs once before all tests.
  // We leave it empty because our integration tests handle their own setup.
  console.log('\nGlobal test setup verified.');
};